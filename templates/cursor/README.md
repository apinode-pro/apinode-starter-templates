# Cursor Setup Starter

Use API NODE wherever Cursor accepts an OpenAI-compatible base URL or custom model provider.

```text
Base URL: https://apinode.pro
Model:    gpt-5.5
API key:  your_api_key
```

Keep the API key in Cursor settings or your local secret manager. Do not paste it into source code.
